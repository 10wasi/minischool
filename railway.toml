# Railway deploy config - ensures correct start command and PORT binding
[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn -w 1 -b 0.0.0.0:$PORT app:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
